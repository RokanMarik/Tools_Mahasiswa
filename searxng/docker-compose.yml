services:
  searxng:
    image: searxng/searxng:latest
    container_name: searxng-fixed
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080/
    volumes:
      - ./searxng-config:/etc/searxng:rw
